//! Code generated by sqlc-gen-rust. SHOULD NOT EDIT.
//! sqlc version: v1.31.1
//! sqlc-gen-rust version: v0.1.1

pub const LIST_AUTHORS_BY_LOCAL_NAMES: &str = r"SELECT id, name, bio FROM authors
WHERE name = $1 OR name = $2 OR name = $3
ORDER BY id";
pub struct ListAuthorsByLocalNamesRow {
    pub id: i64,
    pub name: String,
    pub bio: Option<String>,
}
impl ListAuthorsByLocalNamesRow {
    pub fn from_row(row: &postgres::Row) -> Result<Self, postgres::Error> {
        Ok(Self {
            id: row.try_get(0)?,
            name: row.try_get(1)?,
            bio: row.try_get(2)?,
        })
    }
}
pub fn prepare_list_authors_by_local_names(
    client_: &mut impl postgres::GenericClient,
) -> Result<postgres::Statement, postgres::Error> {
    client_.prepare(LIST_AUTHORS_BY_LOCAL_NAMES)
}
pub fn list_authors_by_local_names(
    client_: &mut impl postgres::GenericClient,
    client: &str,
    statement: &str,
    values: &str,
) -> Result<Vec<ListAuthorsByLocalNamesRow>, postgres::Error> {
    self::list_authors_by_local_names_with(
        client_,
        LIST_AUTHORS_BY_LOCAL_NAMES,
        client,
        statement,
        values,
    )
}
pub fn list_authors_by_local_names_with(
    client_: &mut impl postgres::GenericClient,
    statement_: &(
         impl postgres::ToStatement + ?::std::marker::Sized + ::std::marker::Sync + ::std::marker::Send
     ),
    client: &str,
    statement: &str,
    values: &str,
) -> Result<Vec<ListAuthorsByLocalNamesRow>, postgres::Error> {
    let values_: &[&(dyn postgres::types::ToSql + ::std::marker::Sync)] =
        &[&client, &statement, &values];
    let rows = client_.query(statement_, values_)?;
    rows.iter()
        .map(ListAuthorsByLocalNamesRow::from_row)
        .collect()
}
pub fn list_authors_by_local_names_iter<'c>(
    client_: &'c mut impl postgres::GenericClient,
    client: &str,
    statement: &str,
    values: &str,
) -> Result<postgres::RowIter<'c>, postgres::Error> {
    self::list_authors_by_local_names_iter_with(
        client_,
        LIST_AUTHORS_BY_LOCAL_NAMES,
        client,
        statement,
        values,
    )
}
pub fn list_authors_by_local_names_iter_with<'c>(
    client_: &'c mut impl postgres::GenericClient,
    statement_: &(
         impl postgres::ToStatement + ?::std::marker::Sized + ::std::marker::Sync + ::std::marker::Send
     ),
    client: &str,
    statement: &str,
    values: &str,
) -> Result<postgres::RowIter<'c>, postgres::Error> {
    let values_: &[&(dyn postgres::types::ToSql + ::std::marker::Sync)] =
        &[&client, &statement, &values];
    client_.query_raw(statement_, values_.iter().copied())
}
pub const QUERIES: &[(&str, &str)] = &[("ListAuthorsByLocalNames", LIST_AUTHORS_BY_LOCAL_NAMES)];
