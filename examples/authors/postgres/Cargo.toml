[package]
name = "authors-postgres"
version = "0.1.0"
edition = "2024"

[dependencies]
postgres = { workspace = true }

[dev-dependencies]
test-context = { workspace = true }
test-utils = { workspace = true }
