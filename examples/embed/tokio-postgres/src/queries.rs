//! Code generated by sqlc-gen-rust. SHOULD NOT EDIT.
//! sqlc version: v1.31.1
//! sqlc-gen-rust version: v0.1.12

use tokio_postgres::types::ToSql;
pub struct Authors {
    pub id: i64,
    pub name: String,
}
pub struct Books {
    pub id: i64,
    pub author_id: i64,
    pub title: String,
}
pub struct GetReviewRow {
    pub review_id: i64,
    pub authors: Authors,
    pub books: Books,
    pub rating: i64,
}
impl GetReviewRow {
    pub fn from_row(row: &tokio_postgres::Row) -> Result<Self, tokio_postgres::Error> {
        Ok(Self {
            review_id: row.try_get(0)?,
            authors: Authors {
                id: row.try_get(1)?,
                name: row.try_get(2)?,
            },
            books: Books {
                id: row.try_get(3)?,
                author_id: row.try_get(4)?,
                title: row.try_get(5)?,
            },
            rating: row.try_get(6)?,
        })
    }
}
pub struct GetReview {
    review_id: i64,
}
impl GetReview {
    pub const QUERY: &'static str = r"SELECT r.id AS review_id, a.id, a.name, b.id, b.author_id, b.title, r.rating
FROM reviews r
JOIN books b ON b.id = r.book_id
JOIN authors a ON a.id = b.author_id
WHERE r.id = $1";
    pub fn query_str(&self) -> &str {
        Self::QUERY
    }
}
impl GetReview {
    pub async fn query_one(
        &self,
        client: &impl tokio_postgres::GenericClient,
    ) -> Result<GetReviewRow, tokio_postgres::Error> {
        let stmt = self.prepare(client).await?;
        let row = client.query_one(&stmt, &self.as_params()).await?;
        GetReviewRow::from_row(&row)
    }
    pub async fn query_opt(
        &self,
        client: &impl tokio_postgres::GenericClient,
    ) -> Result<Option<GetReviewRow>, tokio_postgres::Error> {
        let stmt = self.prepare(client).await?;
        let row = client.query_opt(&stmt, &self.as_params()).await?;
        match row {
            Some(row) => Ok(Some(GetReviewRow::from_row(&row)?)),
            None => Ok(None),
        }
    }
    pub async fn prepare(
        &self,
        client: &impl tokio_postgres::GenericClient,
    ) -> Result<tokio_postgres::Statement, tokio_postgres::Error> {
        client.prepare(self.query_str()).await
    }
    pub fn as_params(&self) -> [&(dyn ToSql + Sync); 1] {
        [&self.review_id]
    }
}
impl GetReview {
    pub const fn builder() -> GetReviewBuilder<'static, ((),)> {
        GetReviewBuilder {
            fields: ((),),
            _phantom: std::marker::PhantomData,
        }
    }
}
pub struct GetReviewBuilder<'a, Fields = ((),)> {
    fields: Fields,
    _phantom: std::marker::PhantomData<&'a ()>,
}
impl<'a> GetReviewBuilder<'a, ((),)> {
    pub fn review_id(self, review_id: i64) -> GetReviewBuilder<'a, (i64,)> {
        let ((),) = self.fields;
        let _phantom = self._phantom;
        GetReviewBuilder {
            fields: (review_id,),
            _phantom,
        }
    }
}
impl<'a> GetReviewBuilder<'a, (i64,)> {
    pub fn build(self) -> GetReview {
        let (review_id,) = self.fields;
        GetReview { review_id }
    }
}
pub struct ListReviewsRow {
    pub review_id: i64,
    pub authors: Authors,
    pub books: Books,
    pub rating: i64,
}
impl ListReviewsRow {
    pub fn from_row(row: &tokio_postgres::Row) -> Result<Self, tokio_postgres::Error> {
        Ok(Self {
            review_id: row.try_get(0)?,
            authors: Authors {
                id: row.try_get(1)?,
                name: row.try_get(2)?,
            },
            books: Books {
                id: row.try_get(3)?,
                author_id: row.try_get(4)?,
                title: row.try_get(5)?,
            },
            rating: row.try_get(6)?,
        })
    }
}
pub struct ListReviews;
impl ListReviews {
    pub const QUERY: &'static str = r"SELECT r.id AS review_id, a.id, a.name, b.id, b.author_id, b.title, r.rating
FROM reviews r
JOIN books b ON b.id = r.book_id
JOIN authors a ON a.id = b.author_id
ORDER BY r.id";
    pub fn query_str(&self) -> &str {
        Self::QUERY
    }
}
impl ListReviews {
    pub async fn query_stream(
        &self,
        client: &impl tokio_postgres::GenericClient,
    ) -> Result<tokio_postgres::RowStream, tokio_postgres::Error> {
        let stmt = self.prepare(client).await?;
        let st = client.query_raw(&stmt, self.as_params()).await?;
        Ok(st)
    }
    pub async fn query_many(
        &self,
        client: &impl tokio_postgres::GenericClient,
    ) -> Result<Vec<ListReviewsRow>, tokio_postgres::Error> {
        let stmt = self.prepare(client).await?;
        let rows = client.query(&stmt, &self.as_params()).await?;
        rows.into_iter()
            .map(|r| ListReviewsRow::from_row(&r))
            .collect()
    }
    pub async fn prepare(
        &self,
        client: &impl tokio_postgres::GenericClient,
    ) -> Result<tokio_postgres::Statement, tokio_postgres::Error> {
        client.prepare(self.query_str()).await
    }
    pub fn as_params(&self) -> [&(dyn ToSql + Sync); 0] {
        []
    }
}
impl ListReviews {
    pub const fn builder() -> ListReviewsBuilder<'static, ()> {
        ListReviewsBuilder {
            fields: (),
            _phantom: std::marker::PhantomData,
        }
    }
}
pub struct ListReviewsBuilder<'a, Fields = ()> {
    fields: Fields,
    _phantom: std::marker::PhantomData<&'a ()>,
}
impl<'a> ListReviewsBuilder<'a, ()> {
    pub fn build(self) -> ListReviews {
        let () = self.fields;
        ListReviews {}
    }
}
pub struct ListReviewsByMinimumRatingRow {
    pub review_id: i64,
    pub authors: Authors,
    pub books: Books,
    pub rating: i64,
}
impl ListReviewsByMinimumRatingRow {
    pub fn from_row(row: &tokio_postgres::Row) -> Result<Self, tokio_postgres::Error> {
        Ok(Self {
            review_id: row.try_get(0)?,
            authors: Authors {
                id: row.try_get(1)?,
                name: row.try_get(2)?,
            },
            books: Books {
                id: row.try_get(3)?,
                author_id: row.try_get(4)?,
                title: row.try_get(5)?,
            },
            rating: row.try_get(6)?,
        })
    }
}
pub struct ListReviewsByMinimumRating {
    min_rating: Option<i64>,
}
impl ListReviewsByMinimumRating {
    pub const QUERY: &'static str = r"SELECT r.id AS review_id, a.id, a.name, b.id, b.author_id, b.title, r.rating
FROM reviews r
JOIN books b ON b.id = r.book_id
JOIN authors a ON a.id = b.author_id
WHERE r.rating >= $1
ORDER BY r.id";
    pub fn query_str(&self) -> &str {
        Self::QUERY
    }
}
impl ListReviewsByMinimumRating {
    pub async fn query_stream(
        &self,
        client: &impl tokio_postgres::GenericClient,
    ) -> Result<tokio_postgres::RowStream, tokio_postgres::Error> {
        let stmt = self.prepare(client).await?;
        let st = client.query_raw(&stmt, self.as_params()).await?;
        Ok(st)
    }
    pub async fn query_many(
        &self,
        client: &impl tokio_postgres::GenericClient,
    ) -> Result<Vec<ListReviewsByMinimumRatingRow>, tokio_postgres::Error> {
        let stmt = self.prepare(client).await?;
        let rows = client.query(&stmt, &self.as_params()).await?;
        rows.into_iter()
            .map(|r| ListReviewsByMinimumRatingRow::from_row(&r))
            .collect()
    }
    pub async fn prepare(
        &self,
        client: &impl tokio_postgres::GenericClient,
    ) -> Result<tokio_postgres::Statement, tokio_postgres::Error> {
        client.prepare(self.query_str()).await
    }
    pub fn as_params(&self) -> [&(dyn ToSql + Sync); 1] {
        [&self.min_rating]
    }
}
impl ListReviewsByMinimumRating {
    pub const fn builder() -> ListReviewsByMinimumRatingBuilder<'static, ((),)> {
        ListReviewsByMinimumRatingBuilder {
            fields: ((),),
            _phantom: std::marker::PhantomData,
        }
    }
}
pub struct ListReviewsByMinimumRatingBuilder<'a, Fields = ((),)> {
    fields: Fields,
    _phantom: std::marker::PhantomData<&'a ()>,
}
impl<'a> ListReviewsByMinimumRatingBuilder<'a, ((),)> {
    pub fn min_rating(
        self,
        min_rating: Option<i64>,
    ) -> ListReviewsByMinimumRatingBuilder<'a, (Option<i64>,)> {
        let ((),) = self.fields;
        let _phantom = self._phantom;
        ListReviewsByMinimumRatingBuilder {
            fields: (min_rating,),
            _phantom,
        }
    }
}
impl<'a> ListReviewsByMinimumRatingBuilder<'a, (Option<i64>,)> {
    pub fn build(self) -> ListReviewsByMinimumRating {
        let (min_rating,) = self.fields;
        ListReviewsByMinimumRating { min_rating }
    }
}
